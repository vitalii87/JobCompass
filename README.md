# JobCompass

JobCompass — локальна настільна програма для пошуку, пояснюваного оцінювання та
відстеження вакансій. Готова Windows-збірка не потребує встановленого Python;
вихідний код підтримує Python 3.11+ і використовує стандартну бібліотеку та
один локальний PDF-парсер `pypdf`.

## Що вже працює

- єдині моделі профілю кандидата, вакансії та заявки;
- графічний інтерфейс на `Tkinter/ttk` для Windows;
- локальне завантаження профілю або резюме у форматі JSON, TXT, DOCX чи PDF;
- пояснюваний match score від 0 до 100% із показником повноти доказів;
- консервативне визначення точних дублікатів;
- інтернет-пошук через Bundesagentur für Arbeit, Arbeitnow і Remotive;
- допоміжний імпорт нормалізованих вакансій із локального JSON;
- атомарне локальне JSON-сховище;
- статуси `Found`, `Interesting`, `Draft`, `Applied`, `Rejected`, `Interview`,
  `Offer` та `Archived`;
- фільтри за джерелом, ключовими словами, локацією, remote та score;
- локальна чернетка супровідного листа й структурований промпт для зовнішнього AI;
- повна історія статусів і попередження про можливу повторну подачу;
- додатковий CLI для імпорту, перегляду, оцінювання та зміни статусів.

## Встановлення у Windows

Готові файли створюються у папці `release`:

- `JobCompass-0.8.3-Setup.exe` — звичайний інсталятор для Windows 10/11 x64;
- `JobCompass-0.8.3-Portable.zip` — portable-версія для флешки;
- `SHA256SUMS.txt` — контрольні суми для перевірки цілісності файлів.

Для звичайного встановлення двічі натисніть `JobCompass-0.8.3-Setup.exe`.
Інсталятор не потребує прав адміністратора, додає JobCompass до меню «Пуск»,
створює ярлик на робочому столі та додає коректний деінсталятор. Python і
зовнішні бібліотеки користувачеві встановлювати не потрібно.

Для запуску з флешки:

1. Повністю розпакуйте `JobCompass-0.8.3-Portable.zip` у папку на флешці.
2. Запустіть `JobCompass.exe` з розпакованої папки.
3. Не видаляйте папку `_internal` і файл `portable.flag` та не копіюйте лише
   один EXE — вони необхідні portable-версії.

В інстальованому режимі особисті дані зберігаються у
`%LOCALAPPDATA%\JobCompass\data\jobcompass.json` і не видаляються автоматично
під час деінсталяції. У portable-режимі папка `data` створюється поруч із
`JobCompass.exe`, тому профіль та історію можна переносити разом із флешкою.
Рекомендується періодично робити резервну копію `jobcompass.json`.

Поточні EXE та інсталятор не мають комерційного сертифіката підпису коду, тому
Windows SmartScreen на іншому комп’ютері може показати попередження про
невідомого видавця. Це прибирається лише майбутнім підписуванням релізів
сертифікатом code signing.

## Запуск із вихідного коду

Для підтримки PDF один раз встановіть мінімальну залежність:

```powershell
python -m pip install -r requirements.txt
```

Якщо команда `python` недоступна, використайте `py -3` замість неї.

З кореня репозиторію запустіть GUI:

```powershell
python -m app
```

Або явно:

```powershell
python -m app gui
python -m app gui --data C:\path\jobcompass.json
```

Якщо у Windows доступний лише Python Launcher:

```powershell
py -3.11 -m app
```

Також можна двічі натиснути `run_jobcompass.bat`. Для швидкої перевірки GUI:

1. На вкладці `Профіль` завантажте резюме.
2. На вкладці `Пошук` введіть бажані посади й локації.
3. Виберіть інтернет-джерела та натисніть `Знайти вакансії в інтернеті`.

Пошук виконується у фоновому потоці, тому GUI не зависає. Отримані вакансії
нормалізуються, збагачуються вимогами з німецького опису, дедуплікуються та
зберігаються локально. Кожен новий пошук очищає попередню таблицю й показує
лише поточну видачу; лічильник над таблицею показує її розмір. Історія заявок
при цьому зберігається. JSON-імпорт доступний у `Налаштуваннях` лише як
допоміжний режим.

На вкладці `Результати` можна сортувати вакансії за релевантністю, від нових до
старих або від старих до нових. Дата публікації показується окремою колонкою;
якщо джерело її не передало, вакансія позначається `Не вказано` і при сортуванні
за свіжістю переміщується в кінець списку.

Над таблицею результатів окремо показується діагностика кожного джерела:
скільки вакансій воно повернуло після власних фільтрів або чи сталася помилка.
Arbeitnow переглядає до восьми сторінок останніх європейських вакансій замість
двох; це збільшує покриття, але не замінює спеціалізовані німецькі джерела.
Сторінки Arbeitnow кешуються в пам’яті на 15 хвилин. Якщо наступна сторінка
тимчасово повернула HTTP 429, уже отримані вакансії зберігаються й позначаються
як часткові результати, а не відкидаються повністю.

Повзунок мінімальної релевантності після завершеного пошуку працює локально:
таблиця оновлюється відразу при русі повзунка без повторного звернення до
інтернет-джерел. Якщо над таблицею всі джерела показують `0` або `помилка`,
змінювати поріг немає до чого — спочатку потрібен успішний пошук вакансій.

Формат пошукових фільтрів:

- текстові поля підтримують `Ctrl+C`, `Ctrl+V`, `Ctrl+X`, `Ctrl+A` незалежно
  від активної розкладки клавіатури; правою кнопкою відкривається контекстне меню;
- форма пошуку прокручується колесом миші або клавішами, а кнопка пошуку завжди
  залишається внизу вікна; `Enter` активує вибрану кнопку, `Ctrl+Enter` запускає пошук;
- бажані посади вводяться через кому та працюють як альтернативи
  (`Office Manager, Projektkoordination` означає `Office Manager OR Projektkoordination`);
- додаткові ключові слова необов’язкові; якщо їх кілька, мають збігтися всі
  (`MS Office, SAP` означає `MS Office AND SAP`);
- поле локацій є обов’язковим: почніть вводити місто й оберіть точний варіант
  із підказки, де показано район/землю та країну;
- можна послідовно додати кілька вибраних міст; вони працюють як альтернативи
  (`Köngen OR Stuttgart`), а однакові назви не змішуються завдяки GeoNames ID,
  адміністративним областям і координатам;
- радіус від 0 до 200 км обирається окремим повзунком і застосовується окремо
  до кожного вибраного міста; Bundesagentur отримує його як параметр пошуку;
- вакансії, які Bundesagentur повернув усередині радіуса, не фільтруються вдруге
  за буквальним збігом назви центрального міста;
- конектор Bundesagentur використовує актуальний пошук `/pc/v6/jobs` і зберігає
  сумісність зі старішим форматом відповіді; саме v6 повертає фактичну відстань
  і вакансії навколо вибраного міста;
- якщо сервіс підказок тимчасово недоступний, місто можна явно додати вручну
  без географічної перевірки;
- у поточному локальному JSON-пошуку локація порівнюється за назвою міста,
  оскільки імпортовані вакансії не містять географічних координат;
- якщо `Тільки remote` вимкнено, включаються `remote`, `hybrid`, `office` та
  вакансії, формат яких не вказаний.

Після вибору вакансії кнопка `Підготувати / подати заявку` пропонує:

1. створити локальну чернетку супровідного листа для вибраної вакансії;
2. у безкоштовному режимі сформувати AI-промпт із обробленими даними резюме,
   повним доступним описом вакансії, збігами та прогалинами;
3. продовжити без супровідного листа.

Для промпту можна обрати мову, професійний/теплий/лаконічний тон, довжину та
додати власний правдивий акцент або мотивацію. Окрема вкладка перед копіюванням
показує, які факти кандидата й вакансії буде використано, а також які вимоги не
підтверджені резюме. Email і телефон до промпту не додаються. Промпт вимагає від
AI не згадувати match score чи прогалини у готовому листі та не вигадувати
навички, досвід, освіту, досягнення або дані про компанію.

Чернетку, промпт, використані параметри та огляд даних можна зберегти локально
окремо для кожної вакансії. Після повторного відкриття вакансії JobCompass
відновить збережений варіант. Дані інших вакансій не змішуються. Статус `Applied`
встановлюється лише після ручного підтвердження, що заявка фактично відправлена.

Кнопка `Відкрити вакансію` і подвійний клік по результату відкривають оригінальну
сторінку вакансії в браузері. Відправлення залишається ручним.

## Розуміння німецьких вакансій

Під час JSON-імпорту JobCompass локально аналізує сирий німецький опис, якщо
структуровані вимоги відсутні або неповні. Parser розпізнає секції на кшталт
`Ihr Profil`, `Anforderungen`, `Das bringen Sie mit`, `Wünschenswert` і
`Wir bieten`, а також маркери:

- `erforderlich`, `Voraussetzung`, `fundierte Kenntnisse` — обов'язкове;
- `wünschenswert`, `von Vorteil`, `idealerweise` — бажане;
- `keine Voraussetzung`, `nicht erforderlich` — не вимога.

DE/EN таксономія нормалізує поширені професії, навички та мови, наприклад
`Softwareentwickler ↔ Software Developer`, `Python-Entwicklung ↔ Python` і
`Deutsch ↔ German`. GUI показує цитати з опису, на яких ґрунтується extraction.

Для німецькомовної вакансії локальна чернетка та AI-промпт автоматично
створюються німецькою. Інструкції промпту орієнтовані на природний професійний
стиль німецького ринку: до однієї сторінки, конкретні зв’язки між підтвердженим
досвідом та вимогами, без шаблонних фраз і перебільшень. Мову можна вручну
змінити на `Deutsch` або `English` у вікні супровідного листа.

Підказки міст використовують безключовий Open-Meteo Geocoding API з даними
GeoNames. До сервісу передається лише введений текст міста та вибраний код
країни — резюме, профіль і вакансії не передаються. Дані локацій надаються за
CC BY 4.0; атрибуція: Open-Meteo та GeoNames. Безкоштовний endpoint призначений
для некомерційної версії JobCompass; для майбутнього комерційного режиму
потрібен відповідний тариф або інший геокодер.

Наразі працює безкоштовний режим: JobCompass не викликає AI API, а готує промпт
для перевірки й ручного копіювання. Автоматичний AI/API режим буде доданий у цій
самій програмі окремим налаштуванням; у версії `0.8.3` програма не просить і не
зберігає API-токени.

GUI має п'ять вкладок:

1. `Профіль` — завантаження резюме, перевірка й збереження даних.
2. `Пошук` — інтернет-пошук, вибір джерел і фільтрів.
3. `Результати` — match score, ключові збіги, ризики й дії.
4. `Мої заявки` — поточні статуси та повна історія змін.
5. `Налаштування` — шлях до локального сховища й стан інтеграцій.

Командний інтерфейс залишається доступним:

```powershell
python -m app --help
python -m app init
python -m app import-jobs path\to\jobs.json
python -m app list-jobs
python -m app match path\to\profile.json
python -m app set-status source:id Interesting
python -m app list-applications
```

Під час запуску з вихідного коду локальні дані за замовчуванням зберігаються в
`data/jobcompass.json` і не потрапляють до Git. Інший шлях можна передати перед
командою:

```powershell
python -m app --data C:\path\jobcompass.json list-jobs
```

## Завантаження резюме

- `JSON` завантажується як готовий структурований профіль.
- `TXT` обробляється локально за підписаними секціями на кшталт `Skills:` або
  `Навички:`.
- `DOCX` читається локально стандартними засобами Python.
- `PDF` читається локально через `pypdf`; файли нікуди не надсилаються. Якщо
  PDF зберігає кожну літеру окремо (`K ö n g e n`), JobCompass відновлює слова
  перед витягуванням email, телефону, міста, навичок і мов.
- PDF-скани без текстового шару поки потребують попереднього OCR.

Після завантаження резюме витягнутий профіль одразу зберігається локально та
може використовуватися для пошуку. Ім’я, email, бажані професії та всі інші
ручні поля необов’язкові. Якщо вони порожні, оцінювання спирається лише на
факти, які вдалося знайти у резюме; тому автоматично витягнуті дані бажано
перевірити в GUI. Програма не вигадує відсутні дані.

Загальний відсоток більше не нормалізується до 100% лише за одним доступним
компонентом. Наприклад, точний збіг тільки назви професії обмежується 62% і
позначається як оцінка з низькою повнотою доказів. 100% можливі лише коли
вакансія та профіль містять достатньо незалежних даних для порівняння.

## Формат профілю JSON

```json
{
  "full_name": "Candidate Name",
  "email": "candidate@example.test",
  "phone": "+49 000 000000",
  "summary": "Backend developer focused on reliable local software.",
  "desired_roles": ["Python Developer"],
  "skills": ["Python", "SQL", "Git"],
  "languages": ["English", "Ukrainian"],
  "years_experience": 3,
  "preferred_locations": ["Berlin"],
  "remote_only": false
}
```

## Мінімальний формат вакансій

Файл може містити список вакансій або об'єкт із полем `jobs`. Поля `source`,
`external_id`, `title` і `company` є обов'язковими.

```json
{
  "jobs": [
    {
      "source": "example",
      "external_id": "123",
      "title": "Python Developer",
      "company": "Example GmbH",
      "location": "Berlin",
      "url": "https://example.test/jobs/123",
      "required_skills": ["Python", "SQL"],
      "preferred_skills": ["Docker"],
      "required_languages": ["English"],
      "minimum_years_experience": 2,
      "remote": true,
      "work_mode": "remote",
      "employment_type": "full-time",
      "published_at": "2026-08-01T09:00:00+00:00"
    }
  ]
}
```

## Перевірка

```powershell
python -m unittest discover -v
```

## Створення Windows-релізу

Для відтворення релізу потрібні Windows, Python 3.11+ і безкоштовний NSIS 3.12.
Скрипт сам створює ізольоване `.build-venv`, встановлює build-only залежності,
запускає тести та створює EXE, portable ZIP, інсталятор і SHA-256 checksums:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\build_windows.ps1
```

Збірка використовує PyInstaller у стабільному one-folder режимі. Папки
`.build-venv`, `.tools`, `build`, `dist` і `release` є локальними артефактами та
не потрапляють до Git.

## Конфіденційність і межі прототипу

Профіль, вакансії, збережені матеріали супровідних листів та історія заявок
залишаються у локальному `jobcompass.json`: у `%LOCALAPPDATA%` для інстальованої
версії, поруч із програмою для portable-версії або в `data` під час запуску з
вихідного коду. JobCompass не викликає AI API: кнопка AI-промпту лише готує
текст, який користувач може перевірити та скопіювати. Лише після ручного
вставлення у зовнішній AI-сервіс дані залишають комп’ютер.

У поточній версії немає OCR для PDF-сканів, REST API, embeddings, Ollama,
прямого scraping LinkedIn/Indeed/StepStone або автоматичної подачі заявок. Кнопка відкриття
вакансії передає керування браузеру, а успішну подачу підтверджує користувач.
Поточне онлайн-покриття забезпечують публічні джерела Bundesagentur für Arbeit,
Arbeitnow і Remotive. Нові конектори можна додавати без зміни моделей, matcher
і сховища.
